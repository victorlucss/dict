{
  "name": "dict-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Dict Cloud — Convex backend (auth, cleanup, feature flags)",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.17.0"
  }
}
